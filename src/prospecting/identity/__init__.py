"""Identity resolution: blocking, matching, and merge-conflict resolution.

Collapses the several pages describing one artist into a single canonical record
and reconciles it against the cross-run master file. Without this, one artist
found on three pages becomes three leads and the master file degrades into
duplicates.
"""
