"""Crawler adapters (Firecrawl, direct HTTP fallback).

Turn a URL into text, with caching, ``robots.txt`` compliance, and per-domain
rate limiting. Interpreting the text is the extraction stage's job.
"""
